<!DOCTYPE html>
<html>
  <head>
    <%- include('../partials/head') %>
  <script>
    window.onload = function() {
        (document.getElementsByTagName( 'th' )[1]).click();
        $(".load").slice(0, 10).show(); // select the first ten
        $("#load").click(function(e){ // click event for load more
            e.preventDefault();
            $(".load:hidden").slice(0, 10).show(); // select next 10 hidden divs and show them
        });
    };
  </script>
  <style> 
    .load { display:none; }
    #load { text-decoration: underline; }
    .jumbotron {
      background-color: seashell;
      background-size: cover;
    }
    a {color:rgba(32, 32, 32, 0.733);}
    .mb-3 {
      margin-bottom: 0px!important;
    }
  </style>
  </head>
  <body>
    <%- include('../partials/nav') %>

    <div class="jumbotron">
      <div class="container">
        <h1 class="display-3">Jungwoo Blog</h1>
        <p>I majored in computer engineering from Catholic University of Daegu. 
          I started to study my major with interest as I attended school since 2017. There are really many careers in my major, and it's being used in many fields. 
          I chose this major because I thought I would be able to find a job where I can contribute to an information oriented society, and I'm still learning a lot and thinking about my specific career.</p>
        <p><a class="btn btn-outline-info" href="/about" role="button">About me &raquo;</a></p>
      </div>
    </div>

    <div class="container mb-3">

      <h2 class="mb-3">Recent Posts</h2>
      <br/>
      <table class="board-table table table-sm border-bottom sortable">

        <thead class="thead-light">
          <tr>
            <th scope="col" class="sorttable_nosort">Title</th>
            <th scope="col" class="date" data-sortable="true">Date</th>
          </tr>
        </thead>

        <tbody>
          <% if(posts == null || posts.length == 0){ %>
            <tr>
              <td colspan=2> There is no data to show </td>
            </tr>
          <% } %>
          <% posts.forEach(function(post) { %>
            <tr class="load">
              <td>
                <a href="/menu/posts/<%= post._id %>"><div class="ellipsis"><%= post.title %></div></a>
              </td>
              <td class="date">
                <span data-date="<%= post.createdAt %>"></span>
              </td>
            </tr>
          <% }) %>
          <% seconds.forEach(function(second) { %>
            <tr class="load">
              <td>
                <a href="/menu/seconds/<%= second._id %>"><div class="ellipsis"><%= second.title %></div></a>
              </td>
              <td class="date">
                <span data-date="<%= second.createdAt %>"></span>
              </td>
            </tr>
          <% }) %>
          <% thirds.forEach(function(third) { %>
            <tr class="load">
              <td>
                <a href="/menu/thirds/<%= third._id %>"><div class="ellipsis"><%= third.title %></div></a>
              </td>
              <td class="date">
                <span data-date="<%= third.createdAt %>"></span>
              </td>
            </tr>
          <% }) %>
          <% projects.forEach(function(project) { %>
            <tr class="load">
              <td>
                <a href="/projects/<%= project._id %>"><div class="ellipsis"><%= project.title %></div></a>
              </td>
              <td class="date">
                <span data-date="<%= project.createdAt %>"></span>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
        <a href="#" id="load">Load More</a>
    </div>
  
    <%- include('../partials/footer') %>
  </body>
  
</html>
